name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest

    env:
      STEAM_API_KEY: dummy-key-for-ci
      SENTRY_DSN: ""
      NEXT_PUBLIC_SENTRY_DSN: ""

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci
        working-directory: what-should-we-play

      - name: Lint
        run: npm run lint
        working-directory: what-should-we-play

      - name: Type check
        run: npm run type-check
        working-directory: what-should-we-play

      - name: Test
        run: npm run test
        working-directory: what-should-we-play

      - name: Build
        run: npm run build
        working-directory: what-should-we-play
